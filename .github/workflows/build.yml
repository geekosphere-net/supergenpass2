name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm install --legacy-peer-deps

      - name: Install grunt-cli
        run: npm install -g grunt-cli

      - name: Build (without qunit — needs a real browser)
        run: grunt jshint browserify uglify cssmin staticinline clean checksum

      - name: Verify built files unchanged
        run: |
          git diff --exit-code index.html checksums.json || \
          (echo "Built output differs from committed files — run grunt and commit the result." && exit 1)
